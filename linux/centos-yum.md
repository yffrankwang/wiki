### yum help
	yum search xxx
	yum install xxx
	yum remove xxx
	yum provides xxx.so.##

### list package
	repoquery --list package_name

### downloadonly
	yum --downloadonly --downloaddir=/tmp/ memcached
